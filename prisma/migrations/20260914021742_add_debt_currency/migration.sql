BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Debt] ADD [conversionRateToUsd] DECIMAL(10,6),
[currency] NVARCHAR(1000) NOT NULL CONSTRAINT [Debt_currency_df] DEFAULT 'USD';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
