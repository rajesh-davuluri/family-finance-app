BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[DebtPayment] ADD [currency] NVARCHAR(1000) NOT NULL CONSTRAINT [DebtPayment_currency_df] DEFAULT 'USD';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
